# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-spec/a08f8d51631b7127ddac3d2c65537db429d0f1d0/schema/compose-spec.json

services:
    damage-indicator-devcontainer:
        container_name: damage-indicator-devcontainer
        image: ${IMAGE_REGISTRY:-ghcr.io/imesense}/mcforge1.12.2-damage-indicator-devcontainer:${IMAGE_TAG:-latest}
        platform: linux/amd64

        user: minecraft
        working_dir: /src
        volumes:
            - ${VOLUME_SOURCES:-..}:/src:delegated
            - ${VOLUME_GRADLE:-../.gradle}:/gradle:delegated
        networks:
            - damage-indicator-devcontainer
        command:
            - "tail"
            - "-f"
            - "/dev/null"

networks:
    damage-indicator-devcontainer:
        name: damage-indicator-devcontainer
        driver: bridge
